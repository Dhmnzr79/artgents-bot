# clients/demo/features.yaml — демо для artgents.ru (embed) + demo.bot.artgents.ru (API на VPS)
leads:
  enabled: false
  mode: demo_stub

admin:
  enabled: false

postgres_events:
  enabled: false

guide_router:
  enabled: false

consult_nudge:
  enabled: true

situation:
  enabled: true

price_symptom_consult:
  enabled: true

booking_date_defer:
  enabled: true

messaging:
  free_consultation: true

ingress:
  strict: true

# Brand filter + budget anchor for BRAND_FILTER_ON (demo: classic one-tooth brands)
brand_money:
  enabled: true
  budget_anchor_service_id: classic

# Stage 5a: deterministic numeric fact safety (demo only until eval green)
verifier_gate:
  numeric_fact:
    enabled: true
