# TASK — задание Исполнителю

Один активный `TASK.md` на одну задачу. Клод заполняет на входе; Исполнитель работает строго внутри него.
Общий закон — `.cursor/rules/00-guardrails.mdc`.
Ревьюер (`.cursor/agents/checker.md`) подключается **только на крупных/рискованных задачах** — на мелких Курсор делает сам.

---

## Задача

**Название:** Флип `BRAND_FILTER_ON` в default-ON (demo)

**Размер:** мелкая. Фича уже построена и проверена вживую (коммит `06070a7`), сейчас default OFF. Нужно прогнать сьюту с флагом, убедиться в отсутствии регрессий (у фичи ранний хук в `ask_turn`) и флипнуть дефолт. Петля с Ревьюером не нужна — как коммит `72b1ea2` (флип `PRICE_STRICT_SERVICE_ON`).

## Что сделать

1. Поднять бота локально (`localhost:5000`).
2. Прогнать composer-live-eval с `BRAND_FILTER_ON=1` + канонным набором флагов (`docs/FLAGS_AND_STATUS.md`) + `CLARIFY_STATE_ON=1` (для F1/F2).
3. Проверить результат:
   - Brand-кейсы («какие подешевле», «корейские», «нобель») отрабатывают, `brand_money_path` не `None`, `numeric_fact_gate: pass`.
   - Остальные кейсы (цена, ситуация, медзона, booking, оффтоп) **не регрессировали** против OFF.
4. Если всё зелёно → флипнуть дефолт по устоявшемуся паттерну: env-дефолт `"1"` в `config.py` (`BRAND_FILTER_ON`, строка ~124) + `clients/demo/features.yaml`. Env `="0"` остаётся kill-switch.
5. Обновить `docs/FLAGS_AND_STATUS.md`: перенести `BRAND_FILTER_ON` из «Ждёт решения» в «Проверенные гварды — дефолт-ON».
6. Коммит.

## Затрагиваемые файлы

- `config.py` (дефолт флага), `clients/demo/features.yaml`, `docs/FLAGS_AND_STATUS.md`.
- Артефакт прогона — `eval_*.txt`.

## Явно НЕ делать

- Не рефакторить `ask_turn` «заодно». Не трогать `cesi`/`nikadent`.
- **Не подгонять красные под зелёные.** Если кейс регрессировал — СТОП, не флипать, вернуть владельцу описание регрессии. Флип только на чистой сьюте.
- Опциональное усиление Impro-якоря («чуть дороже» → «рекомендуем/пожизненная гарантия») — **отдельной задачей**, не здесь.

## Готово, когда

Сьюта зелёная с флагом, регрессий нет, дефолт флипнут, доки обновлены, коммит сделан. Если сьюта красная — флип не делается, владелец зовёт Клода.

---

### Шаблон для следующих задач (копировать выше)

```
## Задача
**Название:**
**Размер:** мелкая (Курсор сам) / крупная (петля с Ревьюером)

## Что сделать
1.

## Затрагиваемые файлы
-

## Явно НЕ делать
-

## Готово, когда
```
