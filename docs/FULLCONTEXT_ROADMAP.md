# Full-context Roadmap (audit-driven)

**Статус:** ядро full-context собрано и проверено на demo; впереди — эксплуатационные правки, объединение путей и уборка легаси. Инкорпорирует внешний аудит (2026-07).
**Прогресс (2026-07-03):** Этап 1 ✓ (кэш: cached_tokens 0→17024) · Этап 2 ✓ (история) · 3.0–3.3 ✓ (parity, wiring, контакты, md_chunks) · 3.1c ✓ · **Этап 4 ✓ принят** (плановый вызов: flags-on smoke 24/24, risk 28/29 + r17 закрыт якорем с unit-доказательством, live 16/16; бывшие KNOWN зелёные). **3.4 ✓ принят (2026-07-03):** RAG-стек удалён (−10.6k строк), OpenAI-зависимость исчезла, дефолты флагов ON, композер — единственный content-путь; прогон БЕЗ флагов: product 24/24, risk 29/29, live 16/16. Следующие: остаток 3.1b (display chunk, ограничения клиники в KB, слоты), этап 5 (clarify), 6 (вертикали → pack), 7 (маркетинг-воронка), 8 (гигиена).
**Связано:** `CURRENT_ARCHITECTURE.md`, `ROUTING_MAP.md`, `TECH_DEBT.md`.

---

## 0. Принципы и инварианты (не нарушать ни на одном этапе)

- **База — единственный источник правды.** Бот говорит ровно то, что в базе (md + pricebook), живым языком; факты и цифры — дословно из базы; НЕ выдумывать, НЕ смягчать, НЕ усиливать. Никаких блок-листов «запретных фраз».
- **Деньги/промо/CTA — детерминированно.** Цены из pricebook; промо через `decide_promo_fact` (до LLM, с блокировкой на pain/safety); CTA/кнопки из правил. Модель решает «о чём» и «как сказать», но не «какая цена / какая акция / какая кнопка».
- **Числа сверяются с базой.** `numeric_fact_gate` (whitelist = knowledge base + карточки на fullctx-пути) режет только выдуманные числа; базовые (99.8%, сроки, %) проходят.
- **Fail-open.** Любой новый LLM-шаг при сбое → откат на предыдущее детерминированное поведение. Но **с логированием** (см. этап 8).

## Working agreement

- Один этап = один коммит/PR. Флаги по умолчанию OFF; старый путь не удалять до паритета на eval.
- **Ветки создаёт владелец** — не запускать `git checkout -b`.
- Тесты для composer/full-context шагов: ассертить **структуру/путь**, не слова ответа; LLM мокать; golden не ослаблять.
- **Live-eval гонять В ФОРГРАУНДЕ со всеми флагами** (иначе env не пробросится в фон):
  bash: `export FULLCTX_ON=1 COMPOSER_ON=1 SERVICE_SELECT_LLM_ON=1 ASPECT_PLANNER_LLM_ON=1 E2E_USE_TEST_CLIENT=1 PYTHONIOENCODING=utf-8`
  PowerShell: `$env:FULLCTX_ON="1"; $env:COMPOSER_ON="1"; $env:SERVICE_SELECT_LLM_ON="1"; $env:ASPECT_PLANNER_LLM_ON="1"; $env:E2E_USE_TEST_CLIENT="1"; $env:PYTHONIOENCODING="utf-8"`
  затем `python -m evals.v5.run_composer_live_eval`
  **Внимание (Windows):** если флаги выставлены не в том шелле, live-eval молча пройдёт «на выключенном» — худший вид зелёного. Перед прогоном убедиться, что `answer_path=composer` реально появляется в выводе.

---

## Что уже сделано (ядро)

- **Композер:** пакет (цена/промо/CTA детерминированно) + LLM вплетает один живой ответ на составной вопрос. `orchestration/composer_flow.py`, `core/answer_packet*.py`, `llm.py`.
- **Full-context (шаг 1):** вся база клиента в контексте композера. `core/knowledge_base.py`.
- **Выбор услуги моделью (шаг 2):** `core/service_selector_llm.py` — чинит «generic→дорогой протокол», «удаление→пульпит».
- **База-как-правда:** снесён блок-лист claim_gate из пайплайна; `COMPOSER_TRUTH_STYLE_RULE`; numeric-gate whitelist = вся база (`chunk_responder.py` `_composer_allowed_source_text`).
- Всё за флагами (`COMPOSER_ON`, `FULLCTX_ON`, `SERVICE_SELECT_LLM_ON`, `ASPECT_PLANNER_LLM_ON`), OFF по умолчанию. Live-eval 14/15.

---

## Этапы (в порядке приоритета)

### Этап 1 — Кэш: база в стабильный префикс ⭐ (самая доходная, дешёвая)

**Проблема:** `llm.py:784` `build_messages_for_packet_composer_fullctx` кладёт базу знаний (~30к токенов) в user-сообщение **после** вопроса пациента. Вопрос меняется каждый ход → стабильного префикса перед базой нет → DashScope prefix-кэш **не срабатывает** → полная цена и латентность базы на каждый ход.

**Что делать:**
- Перенести knowledge_base в **system prompt** (стабильная часть), а не в user-сообщение. Порядок в system: identity/style + `RESPONSE_FORMAT` + `COMPOSER_FULLCTX_RULE` + **[БАЗА ЗНАНИЙ]**. Вопрос+аспекты+карточки остаются в user (короткая, меняющаяся часть).
- **Ловушка с динамикой:** system сейчас заканчивается `_consult_nudge_addon(meta)` — он меняется от хода к ходу. Правило: **всё per-turn динамическое (consult_nudge и любые будущие добавки) — строго ПОСЛЕ блока базы или в user-сообщение.** Префикс-кэшу важна только стабильность начала; динамический хвост — ок. Динамика *перед* базой убивает кэш так же, как сейчас.
- Проверить, что DashScope context cache включён/срабатывает (в идеале — залогировать `cached_tokens` из ответа usage, если провайдер отдаёт).
- Заодно (дёшево, из аудита §1.4): в `core/knowledge_base.py` при склейке оставлять **однострочный заголовок на документ** (`## implantation__faq__pain — Боль и анестезия`) для навигации модели и телеметрии.

**Файлы:** `llm.py` (build_messages_for_packet_composer_fullctx, COMPOSER_FULLCTX system сборка), `core/knowledge_base.py`.
**Тесты:** промпт-структура — база в system, вопрос в user; заголовки документов присутствуют в блоке; **между началом system и концом блока базы нет per-turn контента** (consult_nudge — только после базы или в user).
**Проверка:** live-eval (4 флага) — ответы не изменились по сути; замерить время/токены до и после (ожидаем резкое падение стоимости/latency повторных ходов). Прод не меняется (флаги off).
**Коммит:** `perf: knowledge base as stable system-prompt prefix (enable context cache)`

### Этап 2 — История диалога в композер и selector ⭐ (блокер объединения путей)

**Проблема:** композер и `classify_service` НЕ получают историю диалога. Пример «делают all-on-4? → Да → **а сколько стоит?**» сломается, когда композер станет единственным путём.

**Что делать:**
- Передавать в `build_messages_for_packet_composer_fullctx` и в `classify_service` **последние 4–6 реплик** (из `session` / `mem_context`).
- В промпте пометить историю как **«контекст для понимания продолжения, НЕ источник фактов»** (формулировка уже есть в `GENERATOR_SINGLE_SOURCE_RULE`).
- Так «а сколько стоит?» после all-on-4 модель поймёт **без** rewrite-машинерии.

- **Порядок записи памяти не менять:** `respond_from_composer` вызывает `mem_add_user(sid, q)` **после** генерации — история на момент генерации не содержит текущий вопрос (нет дубля «вопрос в истории + вопрос в промпте»). Это требование, а не случайность.

**Файлы:** `llm.py`, `core/service_selector_llm.py`, `orchestration/composer_flow.py` (прокинуть историю), `session.py`/`mem_context`.
**Тесты:** составной follow-up кейс — в messages композера/селектора присутствует блок истории; «а сколько стоит?» после all-on-4 → selector даёт all_on_4 (мок истории).
**Проверка:** live-eval без регрессий. **Управление ожиданиями:** live follow-up кейс («all-on-4 … а сколько стоит?») на этом этапе в композер НЕ попадёт — одноаспектный вопрос уйдёт в легаси через гейт «≥2 аспекта» (до этапа 3). Реальный гейт этапа 2 — unit-тесты с моком истории; live-кейс добавить сейчас как KNOWN/xfail, зелёным он станет после этапа 3. Не «чинить» его раньше.
**Коммит:** `feat: pass recent dialog history to composer and service selector (understanding, not facts)`

### Этап 3 — Объединить пути (single-aspect → композер) + снос RAG

**Проблема:** композер срабатывает только при ≥2 аспектах (`composer_flow.py:89`). 80–90% вопросов («сколько стоит?», «больно?», «где вы?») идут через легаси. Две системы, «магия» только на составных.

**Что делать (по паритету на eval, строго в этом порядке):**

**3.0 — Сначала определить композер-эквиваленты eval-ассертов.** Golden/product сейчас ассертят структуру легаси-retrieval (`meta.source_route`, `doc_id`, выбранный чанк). У композер-ответа **нет выбранного дока** — при переводе одиночных вопросов на композер эти ассерты теряют смысл и golden краснеет по построению. До wiring'а договориться о паритет-метриках композер-пути: `service_id`, факты/суммы из пакета в ответе (T2), `numeric_fact_gate.action == pass`, C1–C6 из live-eval. Обновить ожидания кейсов, **не ослабляя** проверки по смыслу (T5).

**3.1 — Wiring:** пустить **все** content/price вопросы через fullctx-композер, включая одиночные (снять/ослабить гейт «≥2 аспекта» для fullctx). Контакты/врачи/лид — детерминированные маршруты, остаются.

**3.1a — Patient situation / playbook НЕ терять.** Композер-оверлей стоит в `ask_turn.py` **раньше** маршрута `patient_options_overview` — при снятии гейта «≥2 аспекта» ситуационные вопросы («нет одного зуба, что подойдёт?», «нет всей челюсти») уйдут в композер, который про плейбук не знает → **приоритеты клиники молча потеряются**. Решение: `patient_playbook.yaml` материализуется как **карточка пакета** (situation card: варианты в порядке приоритета клиники + позиционирование каждого; выбор и порядок — детерминированно из YAML, модель порядок не меняет). Факты о протоколах композер берёт из базы, базовые цены — из price-карточек. Гейт: eval-якоря `risk_ps01`…`risk_ps09` остаются зелёными (ps09 сейчас красный — добить здесь или в этапе 8).

**3.1b — Инвентарь фич: что обязано пережить объединение.** Проверено по коду (2026-07): CTA/кнопки policy, lead-overlay (finalize_ask), consult_nudge, персист фокуса, promo_overview («какие акции?», pre-resolver), промо-гейт+cooldown, numeric gate — на композер-пути **уже работают**, не трогать. А вот это теряется при снятии гейта «≥2 аспекта» — закрыть в этом этапе:

| # | Фича | Где теряется | Решение |
|---|------|--------------|---------|
| 1 | **Видео, suggest_h3-кнопки, cta_key, `situation_allowed` («Рассказать о ситуации») из frontmatter** | `_composer_display_chunk` угадывает md только по префиксам `implantation__/extraction__/treatment__` — для отбеливания/виниров/ортодонтии display chunk не резолвится → пропадают видео, кнопки «подробнее» и предложение кнопки «Рассказать о ситуации» | Резолвить display chunk из `service_catalog.json` (ref выбранной услуги), не хардкод-префиксами. Сами флоу (показ видео policy, situation collect → лид, PII-маскировка) — pre-resolver/policy, не трогаются |
| 2 | **«Да» по pending / цепочки suggest_h3** | continuation в pre-resolver опирается на `current_doc`; на композер-пути `set_current_doc` срабатывает только если display chunk отрезолвился | То же решение, что #1 (резолв через каталог) |
| 3 | **Answer slots из frontmatter: `clinic_note`, `consult_value`** | композер слоты сознательно не применяет; `promo_note` заменён промо-карточкой, а clinic_note/consult_value молча исчезнут из ответов | Контентная миграция: формулировки → `marketing.yaml` (`clinic_proof`/`consult_reasons` уже есть) → карточка пакета; либо осознанно списать. Решает владелец |
| 4 | **Эмпатия «первое касание темы»** (`is_first_in_topic` + session-флаг) | в композер-промпте механики нет — «одна короткая человеческая фраза при первом заходе на болезненную тему, не повторять каждый ход» теряется | Прокинуть флаг first_in_topic в user-часть промпта композера одной короткой инструкцией (не тащить весь EMPATHY_ADDON) |
| 5 | **Group price defer / unit clarify** («имплантация» без протокола, «сколько за зуб/челюсть») | защита-defer уже есть в `composer_flow`, но завязана на текущий гейт | При снятии гейта «≥2 аспекта» defer в price path сохранить явно; eval-кейс D1 остаётся гейтом |
| 6 | **Followup-кнопки сценариев pricebook** («Что входит», «Этапы оплаты» из S1–S8) | на чистом price-пути есть, на композер-пути с price-карточкой не добавляются | Прокидывать followups сценария из assembler в payload композера |
| 7 | **Ограничения клиники** («детей не лечим», ОМС/ДМС, «базальную не делаем», «Osstem нет», «брекеты → элайнеры») из `clinic_policies.yaml` | детерминированные триггеры в `ingress_gate` ловят только прямые формулировки («дочке 10 лет имплант можно?» — не сработает); **в md-базе этих ограничений нет** (проверено grep'ом) → full-context композер про них не знает и может пригласить ребёнка на приём | Генерировать из `clinic_policies.yaml` компактный блок «Ограничения клиники (чего НЕ делаем)» и включать его в KB-блоб (`core/knowledge_base.py`). Триггеры ingress остаются fast-path; блок в базе — страховка на косвенные формулировки. Источник правды один — `clinic_policies.yaml` |
| 8 | **Честное «не нашла ответа»** (`ui.yaml` → `fallback_menu.low_score` / `no_candidates`) | эти fallback'и — RAG-специфичные (низкий score поиска), умирают со сносом retrieval; композер на вопрос вне базы может «доброжелательно» ответить из общих знаний модели | Правило в `COMPOSER_FULLCTX_RULE`: «если в базе ответа нет — скажи честно, предложи уточнить или связаться с клиникой» + **eval-кейсы на вопросы вне базы** (сейчас в live-eval таких нет). `offtopic`/`empty`/`bare_affirmative` — ingress/pre-resolver, остаются |

Плюс eval-кейс на `price_concern` («дорого…») через композер: промо-гейт по маршруту заблокирует акцию автоматически (allowed_routes), но кейс должен это фиксировать.

**3.1c — Фиксы по итогам первого flags-on прогона product/risk** (найдено parity-харнесом 3.0; smoke 21/24, risk 20/29 на 2026-07-02). Порядок — по приоритету, деньги первыми. Один коммит на пункт, после каждого — flags-on прогон соответствующих кейсов.

| # | Проблема (кейсы) | Причина | Фикс |
|---|------------------|---------|------|
| 1 | **Цена одного протокола вместо обзора группы** (r04, r05: «вся верхняя челюсть», «все зубы под ключ» → только all_on_4, потеряна 398 000) | LLM-селектор **обходит** defer группового прайса в `composer_flow.try_composer_overlay` — defer срабатывает только когда селектор промолчал; селектор нарушает своё правило «группа → null» на scope-вопросах | Детерминированный scope-гард **до** селектора: `patient_scope` = челюсть/все зубы (из `patient_situation` ctx / `patient_scope_cues`) и протокол в вопросе не назван (`_query_names_specific_implant_protocol`) → defer в price route, селектор не вызывать. Unit: обе фразы → defer (LLM мокать); eval r04/r05 |
| 2 | **Врачи уходят в композер** (smoke_10 «Кто занимается имплантацией?») | в 3.1 плейбук передвинут до композера, а doctor-маршрут — нет | В `ask_turn` вызывать `try_a3_doctor_route` **до** `try_composer_overlay` (sr уже посчитан) — зеркально 3.1a. Unit: при `sr.source=='doctor'` композер не вызывается |
| 3 | **Не-ценовые аспекты берут service_id из легаси fuzzy-матча** (smoke_15 рассрочка→all_on_4, smoke_17 гарантия→implant_supported, r17 «во сне»→all_on_4) | `classify_service` зовётся только при price/included; для payment/warranty/pain службу даёт нечёткий каталог-матч — возвращён баг «имплантация → дорогой протокол» | Звать селектор для **всех** аспектов на композер-пути; при null/fail-open **не** брать неавторитетный fuzzy-матч (service_id=None — норм, база покрывает клинику целиком). **Кейсы smoke_15/17, r17 обновить по продуктовой правде:** ассертить `forbidden_service_id` (не дорогой протокол: all_on_4/all_on_6/zygomatic/pterygoid/implant_supported), а не «обязательно classic» — по T1 проверяем то, что важно |
| 4 | **«Коронка на имплант» ≠ обычная коронка** (r09, ps06 → zirconia_crowns вместо implant_supported_prosthetics — разные деньги) | blurb каталога для селектора не различает случаи; плюс «сколько коронка» (без «стоит») не распознан price-аспект | (а) В `service_catalog.json` blurb `implant_supported_prosthetics` начать с «коронка на уже установленный имплант»; (б) эллиптический price: «сколько <термин каталога>» без duration-маркеров → price-аспект (общий паттерн, НЕ под фразу кейса — T4). Негативные тесты: «сколько длится», «сколько заживает» → duration, не price |
| 5 | **«Отдельно оплачивается» не ловится как included** (r08) | `_INCLUDED_ASPECT_RE` знает «что входит/под ключ», но не оплату отдельно | Расширить регекс: «оплачивается отдельно», «отдельно оплачивается», «доплачивать». Unit-тесты с негативами |
| 6 | **Ошибки авторинга ожиданий 3.0 — чинить кейсы, не код** (r11, r16, r13) | parity-блоки писались до включения композера и не проверялись: r11 «циркониевая коронка — это имплант?» ждёт classic/one_stage (бот выбрал zirconia_crowns — верно); r16 аналогично (pterygoid — верно); r13 ждёт aspects=price из эпохи триажа (композер даёт included — семантически правильнее) | Переписать parity r11/r16 по фактической продуктовой правде (service = zirconia_crowns / pterygoid_implants, aspects comparison/overview, контент-сигналы сохранить); r13 → aspects included, прочитать ответ глазами, при корректности снять KNOWN-запись из `TECH_DEBT.md` § Patient situation (долг закрылся сам) |

**Решение по объёму 3.1c (2026-07-02, согласовано с владельцем):** пп. **3, 4б, 5** — НЕ чинить регексами/расширением селектора: это одноразовые мостики, которые этап 4 (плановый вызов) выбрасывает целиком. Вместо этого их кейсы (smoke_15, smoke_17, r17, r08, r09-аспект, ps06) — в `known_failures` сьюта с пометкой «закрывается этапом 4». Делаем только **долгоживущее**: п. 1 (scope-гард — деньги охраняет код и при планировщике), п. 2 (порядок маршрутов), п. 4а (blurb каталога — качество данных), п. 6 (кейсы). **Этап 4 вытягивается вперёд — до 3.4 (сноса RAG):** сначала мозги, потом уборка.

**Гейт 3.1c:** flags-on product + risk: 0 FAIL (PASS + документированные KNOWN); live 16/16; flags-off прогон без изменений (легаси не тронут). Правки кейсов (п. 6) — с комментарием-обоснованием в коммите, это не ослабление, а исправление непроверенных ожиданий (T5-осторожно: контент-сигналы и forbidden-гарды не удалять).

**3.2 — Контакты снять с embed-поиска.** Контактный путь сейчас зовёт `retrieve(q, topk=24)` + `pick_contacts_chunk` (`ask_turn.py:80`) — это embed-поиск на маршруте, который мы «оставляем как есть». Перевести на прямой `get_chunk_by_ref("clinic__info__contacts.md#korotko")` **до** удаления embed-поиска.

**3.3 — Разделить embed-поиск и резолв ref→чанк.** Удаляется только **embed-поиск**: `retrieve`, rerank, alias-матрицы/`.npy`, arbiter. **`get_chunk_by_ref` — НЕ RAG и остаётся:** его используют ~15 выживающих мест (контакты-fallback `ask_turn.py:83`, price flow `price_flow.py:89/183/288`, **материализация карточек самого композера** `answer_packet_materialize.py:45`, payment/warranty appends `answer_plan_apply.py`, lead redirect `lead_flow.py:58`, continuation `pre_resolver_turn.py`, catalog_flow). Вынести `get_chunk_by_ref` в тонкий `core/md_chunks.py`, парсящий `clients/{id}/md/` напрямую — тогда старые индексные артефакты тоже можно убрать, а правки md подхватываются без пересборки индекса.

**3.4-0 — Сначала дефолты флагов ON (часть 3.5, переносится сюда).** Нельзя удалять легаси-путь, пока он дефолтный: `FULLCTX_ON`, `COMPOSER_ON`, `SERVICE_SELECT_LLM_ON`, `TURN_PLANNER_ON` → default "1" (env-переключатель в "0" остаётся как kill-switch). После — полный прогон уже БЕЗ выставления флагов (новые дефолты = прежний flags-on результат).

**3.4-0b — Fail-open политика после сноса (решение):** плановый вызов упал → resolver (он НЕ удаляется); композер упал → честный `LLM_FALLBACK_ANSWER` («не нашла ответа», предложить связаться) — retrieval-путь НЕ реанимируется. Деньги/контакты/врачи/лид — детерминированные маршруты, их fail-open не меняется.

**3.4 — Удаление по графу вызовов, НЕ по списку файлов.** Пример мины: `query_selector.py` выживает (`select_price_service_route` нужен даже композеру для defer группового прайса), но **импортирует** `core/candidate_builder.py` из списка на удаление — снос «по файлам» даст ImportError на старте. Перед каждым удалением — проверить импортёров (grep), после — импорт-smoke (`python -c "import app"`) + eval.

**3.5 — Хвост:** убрать старый композер-форк в `respond_from_chunk`; `EMPATHY_ADDON` уходит с легаси-путём (стилевые правила — да, списки запретов — нет); флаги full-context → default ON.

- **Бонус:** OpenAI-эмбеддинги исчезают (единственная не-китайская зависимость) — «фаза E» закрывается сама.

**Тесты/eval:** полный `run_demo_eval` product/golden/risk + live-eval; паритет без RAG (по метрикам из 3.0). Golden не ослаблять.
**Коммит(ы):** поэтапно — 3.0…3.5 отдельными коммитами, каждое удаление с зелёным eval.

### Этап 4 — Один плановый flash-вызов вместо 4 классификаторов

**Проблема (латентность):** составной ход = ingress → resolver → dialog focus → aspect planner → service selector → композер = **5–6 последовательных** LLM-вызовов; композер на SSE не стримится по-настоящему. Для виджета 6–10 сек = потерянные лиды.

**Что делать:**
- Схлопнуть `resolver` + aspect planner + `classify_service` + dialog focus gray-zone в **ОДИН** flash structured-output вызов:
  `{ route, aspects[], service_id|null, followup_of|null, needs_clarify, patient_situation|null, brand_filter|null }` — модель видит вопрос + каталог + последние реплики и решает всё сразу.
- **`brand_filter` закрывает долг S6** («сколько стоят корейские импланты»): `brand_group` в данных pricebook есть, в маршрутизации — нет (TECH_DEBT § PriceBook #2). Плановый вызов отдаёт `brand|brand_group` → assembler **детерминированно** фильтрует варианты → в price-карточке только запрошенный бренд. Пока фильтра нет, композер по правилу «ДОСЛОВНО» честно выдаёт все бренды — приемлемо, но не отвечает на вопрос точно.
- **Patient situation — в плановый вызов:** regex-детектор `core/patient_situation.py` заменяется полем `patient_situation` (фиксированный enum kind + scope) в structured output — живые формулировки («нет зуба уже лет восемь») модель ловит надёжнее регексов. Плейбук-карточка (этап 3.1a) и price scope (один зуб vs челюсть) начинают питаться от этого поля; выбор и порядок вариантов — по-прежнему детерминированно из `patient_playbook.yaml`.
- **Ingress-гейт (шум/оффтоп) остаётся отдельным** — он в схлопывание НЕ входит.
- **Ограничить радиус поражения:** `DecisionFrame` от resolver'а потребляют route_guards, price_flow, scope-логика, телеметрия — десятки мест. Плановый вызов на первом шаге **материализует DecisionFrame-совместимый объект** (адаптер), потребители не переписываются. Переписывание потребителей — отдельная уборка позже, не в этом этапе.
- Дальше детерминированная сборка карточек (как сейчас) + один вызов композера. **Итого 2 LLM-вызова на ход вместо 5–6.**
- **Настоящий токен-стриминг** композера на SSE-пути (сейчас ответ отдаётся целиком).
- **Нюанс:** при схлопывании теряется per-classifier fail-open → сделать один аккуратный fallback на весь план (при сбое планового вызова → консервативный дефолт/легаси).

**Файлы:** новый `core/turn_planner_llm.py`; `orchestration/*` (заменить цепочку); `app.py` SSE (стриминг).
**Тесты:** плановый вызов (мок) даёт корректный `{route,aspects,service_id,followup,needs_clarify}` на наборе кейсов; fallback при сбое.
**Проверка:** live-eval + **замер сквозного времени ответа** (добавить в eval-гейты как критерий включения).
**Коммит:** `feat: single turn-planner LLM call + real streaming (latency)`

### Этап 5 — Clarify как structured output композера (не state machine)

**Проблема:** старый composer roadmap спроектировал отдельную state machine с bucket-классификатором — избыточно для full-context.

**Что делать:**
- Композер (или плановый вызов из этапа 4) возвращает либо `answer`, либо `clarify { question, option_ids }`. Кнопки — по-прежнему из каталога (инвариант I3). State — крошечный: помнить, что задан вопрос-выбор + набор опций (переиспользовать паттерн lead-flow).
- **Парсинг:** композер сейчас разбирает JSON строго как `{"answer": ...}` (`generate_answer_from_packet_fullctx` в `llm.py`) — схема `answer | clarify` требует править парсинг и SSE-обработку.
- **Риск:** модель может переспрашивать не там → тюнить на eval (кейсы «должен уточнить» / «не должен»).

**Файлы:** `llm.py` (схема ответа + парсинг), `app.py` (SSE), `session.py` (pending-clarify state), `ux_builder.py` (кнопки).
**Тесты/eval:** clarify-кейсы + «да» на вопрос-выбор + уход в другую тему.
**Коммит:** `feat: clarify via composer structured output (question|answer)`

### Этап 5.5 — Единое ядро: карта смыслов (ситуации ↔ услуги) + линзы контент/цена ⭐

**Проблема:** услуги, цены и «что подойдёт» живут в РАЗНЫХ, частично дублирующих структурах: `patient_playbook.yaml` (ситуация→услуги, контент-подбор), `clients/{id}/pricebook/manifest.json` (группы прайса — дубль тех же ситуаций «имплантация/челюсть»), `pricebook/services/*` (цена+бренды), `service_catalog.json`+md (описание). Итог — **два источника правды** для «какие услуги и в каком порядке» (плейбук vs группы) → контент и цена могут разойтись. Плюс параллельный **regex-слой распознавания** (`core/price_scope.py`, `core/patient_scope_cues.py`, `is_generic_implant_price_query`) дублирует то, что плановый вызов (Этап 4) уже отдаёт полями `route/aspects/service_id/patient_situation/brand_filter/followup_of`. Хрупкие имплант-регексы — блокер вертикалей (Этап 6).

**Идея:** одна карта — узлы двух типов (СИТУАЦИЯ, УСЛУГА), несколько ЛИНЗ поверх одних узлов (делаете? / подбор / опиши / сколько), фокус переносится между ходами. Слой понимания = плановый вызов, он уже есть. Этот этап — спина, на которую садятся Этапы 6 (pack) и 7 (воронка); он же поглощает «ценовые комнаты» из рабочих заметок.

**Что делать (по шагам, каждый за флагом, дефолт OFF; старый путь не удалять до паритета):**

**5.5a — Плейбук как единственный источник «ситуация → услуги».**
- `patient_playbook.yaml` = спина карты. Группы `manifest.json` перестают быть отдельным источником: либо **выводятся** из плейбука, либо переносятся в него как ситуации (`full_arch`, `one_tooth`, `bone_deficit`). Порядок/приоритеты — только из плейбука.
- Узел услуги = единый тонкий вид: `service_catalog.json` (описание/md-ref) + pricebook (цена/бренды/этапы). Адаптер `core/service_node.py`; потребители (price_flow, composer) не переписываются.
- Флаг `UNIFIED_MAP_ON`.

**5.5b — Линза: один рендер, четыре проекции.**
- Новый `core/answer_lens.py`: вход = (узел | ситуация) + вид вопроса от планировщика (`exists | select | describe | price`); выход = пакет для композера/карточки.
- «делаете?» → «да» + кнопки ситуации; «подбор» → услуги ситуации кнопками (= нынешний `try_patient_options_overview`); «опиши» → md; «сколько» → детерминированная карточка(и) цены.
- Деньги остаются детерминированными; линза лишь выбирает проекцию одних и тех же узлов.

**5.5c — Фокус = ситуация, а не только услуга.**
- Расширить фокус сессии: хранить и текущую СИТУАЦИЮ, и услугу. «а сколько?» после обзора «нет всех зубов» → цена по набору ситуации; после «классическая» → цена `classic`.
- Опереться на существующие `followup_of` (планировщик) и `last_subject`/`dialog_focus`; добавить situation-focus рядом, не ломая service-focus.

**5.5d — Живые вступления вместо хардкода.**
- Убрать статичные `overview_prompt` и «Выберите протокол ниже» → бриф → композер. Кнопки/цены/порядок пришпилены; живёт только текст. (Вынести вперёд как самый дешёвый первый коммит.)

**Статус 5.5 (2026-07-05):** сделано — 5.5d (живые вступления) ✓ · `core/service_node.py` ✓ · `core/answer_lens.py` (линзы услуги) ✓ · `situation_view` ✓ · **первый вайринг «цена за ситуацию»** (`SITUATION_PRICE_ON`) ✓ · 5.5e (живой intro ситуационной цены на общий `core/living_frame.py`) ✓. Read-only карта (узел → линза услуги → линза ситуации) собрана; цена по ВСЕМ ситуациям плейбука подключена и оживлена, проверено вживую в виджете. 5.5a-1 (конвергенция manifest) НЕ делается — см. уточнение в 5.5a-2. Контент-ре-вайринг «что подойдёт» на карту — отложен (работает на легаси, переписывать рискованно без выгоды). **5.5a-2 ОТМЕНЁН (2026-07-05):** планировщик не отдаёт scope/situation для метод-ценовых вопросов (живой тест: «сколько имплантация» → Скуловая 420k); плюс бот форкается под каждую сферу — мультивертикали в одном боте нет → регекс-routing цен остаётся. Шаги 1a/1b (dormant mapper + флаг) откачены.

**Чистка / удаление легаси (ОБЯЗАТЕЛЬНО; по графу вызовов, НЕ по списку файлов; только после паритета на eval и когда новый путь = дефолт):**

| Что убрать | Чем заменено | Предусловие |
|---|---|---|
| `core/price_scope.py` (`detect_price_scope` + regex-scope) | планировщик `patient_situation`+scope | 5.5a/b дефолт, eval-паритет |
| Имплант-регексы `core/patient_scope_cues.py` (`UPPER_JAW_RX`, `FULL_ARCH_RX`, `ALL_TEETH_MISSING_RX`, `ZYGOMATIC_RX`, `PTERYGOID_RX`, `ONE_TOOTH_*_RX`, `IMPLANT_PRICE_RX`, `query_names_specific_implant_protocol`, `has_price_intent`) | планировщик + карта | то же |
| `core/price_offers.py`: `is_generic_implant_price_query`, `is_full_jaw_implant_price_query`, `is_upper_jaw_restoration_price_query`, `is_one_stage_price_query`, `resolve_implant_group_overview`, `should_offer_unit_clarify`, `build_unit_clarify_answer`, `unit_clarify_quick_replies` | линза + карта | то же |
| Дубль-группы `manifest.json` (implantation/full_jaw/upper_jaw) как отдельный источник | выводятся из плейбука | 5.5a дефолт |
| composer_flow: `_composer_should_defer_group_price`, `_composer_should_defer_jaw_scope_price`, `_query_indicates_jaw_group_price_scope`, `_defer_group_price_via_price_route`, `_SPECIFIC_IMPLANT_PROTOCOL_MARKERS` | линза «сколько» решает scope через планировщик | 5.5b дефолт |
| Хардкод `overview_prompt` / «Выберите протокол ниже» (`price_group_overview.py`) | живой бриф (5.5d) | 5.5d |

**НЕ удалять (несущее):** `get_chunk_by_ref`/`core/md_chunks` (не RAG), pricebook loader, `service_catalog.json`, md-база, numeric gate, детерминированная цена, playbook `answer_style` (`avoid_single_winner`, `avoid_medical_promise`, `mention_consult_ct`). Перед каждым удалением — grep импортёров + import-smoke (`python -c "import app"`) + eval (правило Этапа 3.4).

**5.5a-2 — Снос регекс-routing цен → планировщик (конвергенция; строго по паритету).**

**[ОТМЕНЁН 2026-07-05 — не делаем; см. решение в конце подраздела.]**

**Уточнение цели (2026-07-05):** «два источника правды» — это НЕ manifest-группы vs плейбук. Они данные, частично пересекаются, но отвечают на РАЗНЫЕ вопросы (manifest — «сколько имплантация» как категория; плейбук — «нет зуба, что подойдёт» как ситуация) и **остаются**. Реальный багаж — **регекс-слой routing цен**: `query_selector.select_price_service_route` берёт scope/группу/протокол через `detect_price_scope` (`core/price_scope.py`) + имплант-регексы `core/patient_scope_cues.py` + `is_*_price_query`. Это **дублирует** то, что плановый вызов уже отдаёт (`patient_situation`, `scope`, `service_id`, `brand_filter`), хрупко и блокирует вертикали. Значит цель уборки — **регексы, не данные** (строка таблицы выше про «дубль-группы manifest» — отменяется, manifest остаётся).

**Порядок (нельзя удалять до паритета):**
1. **planner-routing за флагом `PRICE_ROUTING_FROM_PLANNER`.** В `select_price_service_route` scope/group_id/protocol/brand берутся из планировщика (`turn_plan_from_ctx`); `detect_price_scope` — только fail-open (планировщика нет / флаг off). Флаг off = байт-в-байт.
2. **паритет-харнесс:** на наборе ценовых запросов решения planner-routing == регекс-routing (эталон — существующие `test_price_scope_router.py` / `test_price_offers.py` + live-набор). Расхождение → чиним подсказку планировщика или фиксируем как осознанное улучшение (T5). Это гейт включения.
3. **флип дефолта** `PRICE_ROUTING_FROM_PLANNER=1`, полный прогон без выставления флага.
4. **снос по графу вызовов:** `core/price_scope.py`, имплант-регексы `core/patient_scope_cues.py`, `is_generic_implant_price_query`/`is_full_jaw_*`/`is_upper_jaw_*`/`is_one_stage_*`, `resolve_implant_group_overview`, `should_offer_unit_clarify`, defer-хелперы `orchestration/composer_flow.py`. Перед каждым — grep импортёров + `import app` + eval; их тесты переписать на planner-routing, смысл не ослаблять.

**Инвариант:** D1 сохраняется — «сколько имплантация» → обзор протоколов, просто решение принимает планировщик, не регекс. Manifest-рендер (`build_group_overview_answer`) не трогаем.

**Коммиты (по шагам):** `feat: price routing from turn planner (PRICE_ROUTING_FROM_PLANNER, parity fail-open)` → `test: price routing parity harness (planner vs regex)` → `chore: default price routing to planner` → `refactor: retire price_scope regexes and implant price-query cues (by call-graph)`.

**Решение (2026-07-05):** отменяем. Планировщик понимает СИТУАЦИИ пациента («нет зубов»), но не распознаёт простые метод-ценовые вопросы по названию услуги («сколько имплантация») — их scope дают регексы, и заменять их планировщиком делает routing хуже (доказано вживую). Плюс мультивертикальность решается ФОРКОМ бота под каждую сферу, а не абстракцией общего кода → дентал-регексы остаются в дентал-боте. Оставляем: price-scope регексы (`price_scope.py`, `patient_scope_cues.py`, `is_*_price_query`). Ядро 5.5 (service_node → линзы → situation_view → living_frame) — переиспользуемый движок, его наследует будущий форк под новую сферу.

**Файлы:** новые `core/service_node.py`, `core/answer_lens.py`; `patient_playbook.yaml` (спина); `orchestration/ask_turn.py`, `orchestration/composer_flow.py`, `orchestration/price_flow.py` (маршрут через линзу); `session.py` (situation-focus); `core/turn_planner_llm.py` (вид вопроса `exists|select|describe|price`, если ещё не выводится); `llm.py`/`core/price_group_overview.py` (живой бриф).

**Инварианты (не нарушать):** D1 (обзор протоколов на «сколько имплантация» сохраняется); деньги — только из прайса, композер цифр не пишет; playbook-правила безопасности не ослаблять; порядок услуг — из плейбука, модель не меняет; fail-open — сбой линзы/карты → откат на текущий путь; всё за флагами, дефолт off; **никаких новых блок-листов** — гейт позитивный.

**Тесты/eval:** линза (мок) даёт правильную проекцию на 4 вида вопроса; «а сколько» после обзора ситуации → цена набора (не одной услуги); «делаете имплантацию?» → exists+кнопки; паритет playbook/price по метрикам 3.0; live-eval без регрессий. Golden не ослаблять.

**Коммиты (по шагам):**
- `feat: living overview intro via composer (retire hardcoded overview_prompt)` (5.5d — первым, дёшево)
- `feat: unified situation↔service map, playbook as single source (UNIFIED_MAP_ON)` (5.5a)
- `feat: answer lens — content/price projection over one map` (5.5b)
- `feat: situation-level focus carry for price follow-ups` (5.5c)
- `refactor: retire implant-specific scope regexes and duplicate price groups` (чистка, после паритета)

**Связь с роадмапом:** спина для Этапа 6 (остаток вертикальных cues уезжает в pack сам) и Этапа 7 (воронка садится на карту ситуаций). Порядок: 5 (clarify) → 5.5 (ядро) → 6 → 7.

**Приложение к 5.5 — карта комнат/ситуаций (данные, ведёт владелец).** ~12 общих формулировок сводятся к **5 комнатам + фильтр бренда + прямые ответы**. Принципы: одна ось на комнату; каждая услуга — ровно в одну ветку (тест против каши); шаг-вопрос кнопками — **только если ось не названа** в запросе (названа → сразу в ветку); всегда кнопка «Не знаю» → консультация; врачебные различия (диагноз, кость, какой протокол) не спрашивать, а показывать вилкой. Комнаты восстановления = существующие правила плейбука (не плодить заново); «Лечение» и «Эстетика» — новые, ценовые.

| Комната | Ось | Источник (playbook rule) | Услуги по веткам |
|---|---|---|---|
| **Зубов нет** | объём → опора | `one_tooth_restore`, `few_teeth_restore`, `full_arch_restore` | один: `classic`, `one_stage` · несколько: `implant_supported_prosthetics`, `classic`, `clasp_dentures`, `removable_dentures`, (мост `zirconia_crowns`) · челюсть: `all_on_4`, `all_on_6`, `removable_dentures` |
| **Сложные / нет кости** | показать + мостик | `bone_deficit_solution` | `sinus_lift`, `zygomatic_implants`, `pterygoid_implants` |
| **Лечение зуба** | показать (решает врач) | новое (в плейбуке нет) | `caries`, `pulpitis`, `periodontitis`, `teeth_treatment`, `tooth_extraction`, (коронка `zirconia_crowns`) |
| **Эстетика** | ценность: цвет/форма/ровность | новое | `professional_whitening`, `veneers`, `aligners`, (`zirconia_crowns`) |

Поведение по комнатам:
- **Зубов нет** — шаг-вопрос `[Один зуб] [Несколько] [Вся челюсть] [Не знаю]` только если объём не назван. Внутри «несколько/челюсть» — вторая ось «опора» (импланты / мост / съёмный). Протокол (all-on-4 vs all-on-6, классическая vs одномоментная) — **показать вилкой**, не спрашивать. Герой = цена; альтернатива = словами, точная цифра за тапом.
- **Сложные / нет кости** — режим «успокоить + показать варианты по КТ + мостик», НЕ меню-выбор.
- **Лечение зуба** — одна тёплая вилка «от …» + консультация. Кнопок-диагнозов (кариес/пульпит) нет.
- **Эстетика** — шаг-вопрос `[Белее] [Форма передних] [Выровнять] [Не знаю]` только для размытого «красивые зубы»; при названной услуге — прямой ответ.

**Фильтр бренда (не комната):** модификатор поверх любого имплант-ответа (`classic`/`one_stage`/`all_on_4`/`all_on_6`) + сигнал ценовой чувствительности (корейские→бюджет, Nobel→премиум). Бренды: Implantium (Корея) · Impro (Германия, реком.) · Nobel (Швейцария). Бренд без объёма → сперва объём, потом фильтр.

**Прямые ответы (не комната):** названа конкретная услуга → карточка сразу, без комнат/вопросов — `veneers`, `professional_whitening`, `tomography` (КТ), `tooth_extraction`, `zirconia_crowns`, `aligners`, `clasp_dentures`, названные протоколы (all-on-4, классическая, скуловая).

**Покрытие (проверка):** все 21 услуги распределены; `zirconia_crowns` появляется в 3 комнатах — не каша, т.к. комнаты входят по разной ситуации (нет зуба / зуб разрушен / хочу красиво); `all_on_4/6` в «Сложных» — ссылка в «Зубов нет», не дубль выбора.

### Этап 6 — Вертикальные хардкоды → client pack (мультивертикальность)

**Проблема:** в общем коде зашиты стоматологические регексы — блокер подключения «клиник красоты»:
- `_SPECIFIC_IMPLANT_PROTOCOL_MARKERS` (`composer_flow.py:21`), `is_generic_implant_price_query`;
- `core/patient_scope_cues.py` (зубные cues);
- `_TOPIC_CONTENT_REF` только под имплантацию (`core/answer_packet.py`).

**Что делать:**
- Многое из этого **исчезает** после этапов 3–4 (селектор заменил маркеры, full-context — content-refs). Остаток вынести в **client pack** (`clients/{id}/routing.yaml` / features), чтобы общий код не знал слов «имплант».
- Критерий готовности: подключение 2-й вертикали = добавить папку клиента, без правок общего кода.

**Коммит:** `refactor: move vertical-specific cues from shared code to client pack`

### Слабые места и порядок (перед Этапом 7 — аудит 2026-07-06)

Приоритет (дёшево+важно → крупно):
1. **Логи fail-open (первым, дёшево).** `except Exception: return None` в `orchestration/composer_flow.py` (`try_composer_overlay`, `_defer_group_price_via_price_route`) — добавить `log_json` в каждый except. Без этого отладка «почему схема не сработала» (Этап 7) — вслепую. (`core/living_frame.py` уже логирует — доделать оверлей.)
2. **Постоянный паритет-гейт цен (дёшево).** Регекс-routing и планировщик сосуществуют (5.5a-2 отменён, регексы — правда). Держать набор ценовых запросов (`tests/test_price_scope_router.py` + live) как **постоянный** eval-гейт, чтобы новые формулировки не разошлись между слоями.
3. **Clarify — мерж + включение (рядом с Этапом 7).** Композер-clarify (переспрос «один зуб или челюсть?») **построен, но на несмерженной ветке `clarify`** — в этой ветке/`main` его НЕТ (`CLARIFY_STATE_ON` отсутствует; `plan.needs_clarify` — write-only в ctx; `DecisionFrame.needs_clarification` не читается). Правка = **мерж/портирование ветки `clarify` + включить флаг + тюнинг на eval**, НЕ билд с нуля. Схемы маркетинга без переспроса неполны → делать вместе.
4. **Латентность / стриминг (крупно, отдельным заходом).** Фактически 4–6 последовательных LLM-вызовов на контентный ход (ingress + patient_situation + dialog_focus + query rewrite + композер); SSE отдаёт ответ целиком (`app.py`, no text_delta). Для лид-виджета — самый заметный юзеру дефект. **Помнить:** `LIVING_OVERVIEW_ON` добавляет ещё plus-вызов → тепло vs скорость, включать осознанно и мерить.
5. **KB-cache без mtime — НЕ приоритет (решение владельца 2026-07-06).** `core/knowledge_base.py` не инвалидирует кэш → правка md видна после рестарта. **Владелец: терпимо** — базу правит только Denis/помощник, не заказчики; рестарт при правке приемлем. Беклог, не блокер.
6. **Ситуационный контент на легаси-chunk (не трогать, но помнить).** `patient_options_overview` → `kind="chunk"` → `respond_from_chunk` (не композер). **Инвариант Этапа 7:** концерн-схемы вешать ТОЛЬКО на композер, не на этот маршрут (иначе вторая реализация маркетинга). Ситуационная ЦЕНА уже на композере; на chunk-пути только ситуационный контент.
7. **Мелочи (беклог):** `core/claim_gate.py` (мёртвый), `risk_ps09` красный, Windows-ротация логов, one_tooth playbook / urgent slice, `build_index.py` хвост, docs backlog-заметка не закоммичена.

**С чего начинать новую ветку:** №1 (логи) + №2 (паритет-гейт) — дёшево, разблокируют. Потом №3 (clarify мерж) вместе с Этапом 7. №4 (латентность) — отдельно.

### Этап 7 — Маркетинг: концерн-схемы как слой над композером

**Проблема:** маркетинг реактивный (промо-гейт, `clinic_proof`/`consult_reasons`, CTA) — нет продающих «схем хода» как конфига и нет режиссуры объекций/страхов.

**Модель (спроектировано в чате 2026-07-06):**
- **Принцип:** код решает ЧТО (схема), LLM пишет КАК (слова), деньги пришпилены. НЕ новый движок — слой-бриф над композером (приём `living_frame`).
- **Поток:** планировщик классифицирует `concern` (`pain|durability|safety|price|null`) → **код** выбирает схему (карта в `marketing.yaml`) → схема несёт `tone` + `beats` (ссылки на **fact-id**) → композер плетёт слова из md-базы + вставляет пришпиленные факты из `pricebook/facts.json` → рендер woven/mixed + кнопки.
- **Схем МАЛО (~2):** `reassurance` (страх/приживаемость/безопасность — арка generic, факт темо-зависимый из базы) и `price_doubt` (объекция по цене). Арка переиспользуется, конкретика из базы → не плодить схему на тему.
- **Beats = fact-id** (`implant_warranty`, `all_on_installment`, `tax_deduction` — уже в `facts.json`), **НЕ md#anchor** (хрупко). Деньги/термины — только пришпилено; качественное успокоение — композер из базы.
- **Триггер — поле `concern` от планировщика** (не метка в документе, не «настроение»): фикс-ярлык, проверяемый на eval. Документы md НЕ меняются (банк фактов + `suggest_h3` + флаги).

**Гардрейлы:** одна схема/ход · cooldown · **fail-open** (промах классификации → обычный ответ + крючки, не поломка) · медбит в arke · факты только из данных · **вешать только на композер** (слабое место №6).

**Крючки + closer (для не-концернов):** универсальный маленький набор чипсов, применимость **выводится из данных услуги** (есть цена → «этапы/рассрочка»; есть видео → «видео»; дорогая → «рассрочка» вперёд) + универсальный мягкий closer на контентных ответах. НЕ схема на тему.

**Первые шаги:** (1) слабое место №1 (логи) — до всего; (2) `concern`-классификация в планировщик + eval-кейсы (как `price_concern`); (3) 2 схемы в `marketing.yaml` (beats=fact-id, tone); (4) селектор `concern→схема` + инъекция брифа в композер за флагом, fail-open.

**Инварианты:** деньги детерминированы; порядок/факты из данных; медзона → только handoff (см. карту интентов); всё за флагом, дефолт off.

**Коммиты:** `feat: concern classifier in turn planner` → `feat: marketing schemes (concern → arc, fact-id beats)` → `feat: scheme brief injection into composer (flag)`.

**Приложение к 7 — карта интентов (эталон поведения на входы).**

Правило против разбухания: **интент ≠ аспект ≠ механизм.** Интент = строка (стенс+владелец); аспект (сравнение/этапы/рассрочка) вплетает композер по тегу; механизм (перенос фокуса, мульти-аспект, крючки) — сквозной. **12 интентов:**

| Интент | Пример | Стенс | Владелец | Статус |
|---|---|---|---|---|
| Контент-вопрос | «что такое all-on-4?» | объяснить + крючки + closer | композер | ✅ |
| Концерн | «почему дорого», «боюсь» | продающая арка (по `concern`) | схема | 🟡 Этап 7 |
| Ситуация | «нет зубов, что подойдёт?» | варианты по приоритету + мостик | playbook | ✅ |
| Доверие | «врачи опытные?», «отзывы?» | proof: врачи, гарантия, кейсы | doctors/proof | 🟡 |
| Простая цена | «сколько удаление?» | точная цена из прайса | price card | ✅ |
| Сложный расчёт | «посчитайте 3 зуба» | per-unit + «точное на КТ» (не перемножать) | price fallback | 📋 backlog |
| Двусмысленность | «4 импланта» | вилка + переспрос | clarify | 🟡 на ветке `clarify`, не смержено |
| Оффтоп | «какая погода?» | мягкий редирект | ingress | ✅ |
| Вне базы | «делаете брекеты?» | честно «не делаем» + альтернатива | out-of-base | 🟡 |
| Админ-инфо | «где вы?», «часы?» | короткие факты | contacts | ✅ |
| Готов к лиду | «запишите меня» | сбор контакта | lead flow | ✅ |
| **Проблемное/несервисное** (жалоба · острое медицинское · перенос записи) | «мне не перезвонили», «опухла щека», «хочу перенести» | **один шаг → администратор, БЕЗ советов**; срочное → «не ждите, звоните сразу» | handoff (`manual_contact`) | ✅ по сути |

**Медзона — жёсткий safety-инвариант:** острое/жалоба/пост-лечебное → **handoff в один шаг, никаких советов/диагноза/оценки.** Реализовано (`manual_contact` + `is_urgent` + urgent-суффикс «не ждите, звоните сразу», телефон подставляется из `clinic_policies.yaml`). 2 мелкие дыры (не safety): мягкое пост-лечебное сейчас идёт контентом (решение владельца); «перенос записи» отдельного handoff не имеет.

**Аспекты (не строки):** цена · рассрочка · гарантия · боль · что-входит · длительность · сравнение · этапы · обзор · подготовка. **Механизмы (не строки):** перенос фокуса (`last_subject`/`followup_of`) · мульти-аспект (композер) · крючки+closer · concern-overlay · бренд-фильтр.

### Этап 8 — Гигиена и надёжность (можно параллельно)

- **`data/cesi/bot.db` в git** — SQLite сессий, **потенциально телефоны лидов**. СНАЧАЛА посмотреть, что внутри; если PII — вынуть из истории git, не только `.gitignore`. Так же старые бинарные артефакты индекса (умрут с RAG), `eval_*_last.txt` из корня.
- **Логировать fail-open:** `except Exception: return None` в `composer_flow.py:160` и `_defer_group_price_via_price_route` — добавить `log_json` в каждый except (иначе падения композера в проде невидимы).
- **Удалить мёртвый `core/claim_gate.py`** + ссылки в `run_composer_live_eval.py` (блок-лист снесён, файл остался).
- **`_KB_CACHE` инвалидация по mtime** папки `clients/{id}/md/` (иначе правка md не видна до рестарта).
- **`risk_ps09`** (маршрут `patient_options_overview` не срабатывает) — добить красный.
- **Сквозное время ответа** — добавить в eval-гейты как критерий включения (см. этап 4).
- **Windows-safe ротация логов:** `PermissionError [WinError 32]` при ротации `logs/demo-app.jsonl`, когда файл держат два процесса (eval + живой бот). Не критично (logging глотает и продолжает), но спамит traceback на каждой записи. Лечение: concurrent-safe handler или ротация по времени с retry/skip; заодно поднять maxBytes.

---

## Порядок и логика

1 (кэш) и 2 (история) — **дешёвые, разблокируют больше всего**, делать первыми. 3 (объединение+снос RAG) — главный выигрыш по коду. 4 (плановый вызов) — латентность, после объединения. 5–7 — новые фичи **на композере, не на легаси**. 8 — параллельно.

**Корректировка порядка (2026-07-02):** этап 4 (плановый вызов) выполняется **до** шага 3.4 (снос RAG) — flags-on прогоны показали, что regex-детектор аспектов и выбор услуги дают класс ошибок, который системно закрывает только планировщик (см. 3.1c: KNOWN «закрывается этапом 4»). Порядок: 3.1c-минимум → этап 4 → снятие KNOWN → 3.4 снос → 3.5.

**Мета-правило:** новые фичи (clarify, воронка) строить сразу на композере/плановом вызове, а НЕ на старой многослойной машинерии — тогда уборка удешевляет каждую следующую фичу.

---

## Backlog (заметки на будущее — не staged)

### Честная обработка сложных ценовых запросов (без калькулятора)

**Класс запросов:** «нет 3 зубов, посчитайте», «сколько за несколько зубов» — количественные / мульти-юнит ценовые запросы **вне** схемы «одна услуга / известная группа / ситуация зуб-челюсть».

**Проблема:** точную СУММУ дать нельзя (нужно умножение + правила пакетов/скидок), а LLM деньги считать **НЕ должен** (деньги детерминированы, numeric gate режет вычисленные числа). Детерминированный калькулятор (`N × цена` с реальными правилами клиники) — сложно и рискованно, **откладываем.**

**Fallback (пока):** для таких запросов отдавать **цены ЗА ЕДИНИЦУ** из pricebook (пришпилено) + качественная рамка вариантов + детерминированный closer «точную сумму врач посчитает по КТ, бесплатно на консультации». Бот **ничего не перемножает**; точный total = консультация. **Ограничение бота превращается в воронку.**

**Инварианты:** LLM не считает деньги; per-unit цены только из pricebook; **никогда** не отфутболивать «зависит от многого» — всегда реальный per-unit ориентир (конкретика = доверие).

**Где:** ветка price-path при scope = quantity/multi/unresolved (в `price_flow`), за флагом, fail-open; LLM только плетёт слова, числа пришпилены per-unit.

**Позже (опционально):** детерминированный калькулятор (`N × цена` + костная пластика «отдельно») — но с **реальными правилами прайса**, не наивным `×N` (иначе промажет мимо пакетных цен). Только когда данные прайса это поддержат.
