# D2 — действующая дорожная карта демо-диалога

## Текущий порядок восстановления — 2026-09-26

Продолжается существующая D2-задача в единственной активной папке
`C:\Cursor Projects\artgents-bot-active`, ветка `codex/d2-stage1-contract`.
Исходный runtime baseline: `e261383515d94e7d925acc705d8a6731aa704e48`.
`origin/main` и merge-base: `141ce91fb1731cd990fcf8391550150016c73e7f`.
Старые папки и ветки — сохранённые источники сверки, не места новой работы.
Документы сохранены в `a185b54`. После их review владелец отдельно разрешил
REC-1 на этой ветке и установку только шести тестовых пакетов в active `.venv`.
REC-1 после независимых Checker/Cursor review сохранён и отправлен в ветку:
`f4bae9b0b292026733854ae1d8fd34e608f953d5`. Владелец разрешил подготовить
карточку REC-2, затем Checker и Cursor подтвердили её отдельно. Документальный
checkpoint — `ce47c16f564498165c1d00b2d0efd997dcbb9c22`; после него владелец
разрешил реализацию REC-2 в active-папке. Diff REC-2 пока Draft и ожидает
независимых Checker/Cursor проверок. REC-3–5, перенос старой ветки, live,
merge и deploy не разрешены.

Эта последовательность уточняет устранение пробелов этапов 1–4 перед
закрытием этапа 5, а не создаёт вторую архитектуру или конкурирующий roadmap.
Исторические PASS сохраняются как evidence своих checkpoint, но не означают
готовность нынешних целых диалогов. Статусы и ограничения записаны в
[Ledger](DEMO_D2_CHECKPOINT_LEDGER.md), состав сохранённой работы — в
[реестре сверки](DEMO_D2_RECONCILIATION_INVENTORY.md).

| Шаг | Ограниченный результат | Ворота |
|---|---|---|
| REC-1 — диагностика | Безопасные причины, шаги и исходы текущего D2, без переписки и без изменения ответов | [Отдельная карточка](DEMO_D2_RECOVERY_DIAGNOSTICS_TASK.md), согласование GO и тестового окружения; Checker и Cursor |
| REC-2 — ответы и ссылки | Пригодная живая проза не отбрасывается из-за необязательного provenance; строгие tenant/UI/lead/privacy границы сохраняются | [Карточка и implementation evidence](DEMO_D2_RECOVERY_CONTENT_TASK.md); GO получен, diff Draft; до закрытия независимый Checker и Cursor. Остаточные baseline/старые read-only тесты указаны в карточке |
| REC-3 — память | Однозначно выбранная точная услуга доходит до сохранённого состояния и следующего вопроса | Отдельная карточка; multipart не выбирает фокус произвольно; follow-up, смена темы, CLARIFY и TTL |
| REC-4 — цены и кнопки | Различимые предложения, краткая цена с существенными условиями, чистые подписи и нейтральная вводная обзора | Отдельная карточка; exact-service ascending без лимита отдельно от authored overview до 3; не скрывать важные условия |
| REC-5 — общая приёмка | Одна сверенная сборка проходит полные семьи A/B/C, JSON/SSE/widget, replay, tenant/lead/privacy | Независимый Checker и финальный Cursor; затем отдельно согласованные live-вызовы с бюджетом и ручной widget review |

После каждой правки проверяются целые затронутые диалоги: текст, UI,
состояние следующего хода и replay. Такие проверки не откладываются до REC-5.
Ни одна следующая строка не является разрешением править её файлы заранее.
Нет автоматического cherry-pick/merge `b8b28d3`, возврата legacy finalizer,
нового public wire или raw transcript ради журналирования.

## Историческая точка отсчёта — документальный этап 0, 2026-09-24

Этап 0 начинался от сохранённого D2-коммита `38fdeb3` в отдельной чистой ветке
`codex/d2-stage0-docs`. Основная папка на `codex/demo-d2-service-volume`
содержит незакоммиченный D2 WIP: он не включён в baseline этого документа,
не объявлен проверенным и не переносится целиком без отдельного разбора.
`origin/main` (`141ce91`) не содержит последующие 71 D2-коммитов. Владелец
явно разрешил использовать D2 baseline и временный worktree для этапа 0;
это исключение из обычного правила AGENTS.md о новой задаче от `origin/main`.

Локальные `/ask`, `/ask/stream` и widget уже вызывают D2, а FullContext уже
собирается. Подключённый путь ещё не означает устойчивый диалог. В production
нет бота и пользователей. Старый normal-dialogue runtime не является целью
совместимости; скрытый fallback к нему запрещён. Прежние R1–R7 и CP5–CP8
ниже — исторические записи, а не порядок новой работы.

## Цель и границы текущей работы

D2 — бот продаж клиники, не персональный медицинский консультант. Обычный
вопрос и клик по follow-up получают живой ответ модели по полному
утверждённому корпусу текущей клиники. Услуга, исходное намерение, явно
названный объём и проверенный ref действия доходят до модели и сборки
ответа без повторного угадывания по подписи кнопки. Отдельные смысловые
маршруты «страх», «сравнение», «сомнение» и ветки под фразы не вводятся.

Код владеет проверенными ценами, предложениями, акциями, политиками,
контактами, UI, lead/privacy, tenant ownership, хранением и replay.
Один ответ может объединять живую информацию и эти структурированные блоки.
Неточность живой прозы, даже обнаруженная коммерческая, не вырезается,
не переписывается и не блокирует пригодный ответ: событие фиксируется
с trace_id в существующем локальном D2-журнале. Существующая админка
работает со старой архитектурой; её интеграция с D2 — отдельная будущая
задача. Возможное противоречие фразы модели и проверенного блока —
осознанный демо-риск, не гарантия полной семантической проверки. Это
послабление не отменяет строгих tenant, privacy, UI-action и lead/effect
границ. Неразбираемый ответ без пригодного текста остаётся технической
ошибкой и диагностируется.

## Порядок этапов и ворота

| Этап | Результат | Доказательство до следующего этапа |
|---|---|---|
| 0 — документы | Согласованы roadmap, договор, решения, приёмка и короткий prompt; исторические планы отделены. | Только документальный diff; ссылки и противоречия проверены; независимый Checker PASS и отдельный Cursor review. Код, данные и тесты не меняются. |
| 1 — смысловой контракт | Согласованы фактический FullContext prompt, typed envelope, production parser, session binding и D2 gate. Необязательное поле не уничтожает пригодный живой ответ; второго prompt/parser/selector нет. | Настоящий prompt builder на tenant snapshot, raw fake output через production parser и общий D2 turn; допустимые и неполные формы. FullContext проверяется, не реализуется заново. |
| 2 — память и UI | Сохраняются услуга, явно названный объём, исходная задача незавершённого уточнения и достаточная очищенная история. Follow-up использует проверенный ref; ценовой клик сохраняет ценовой вопрос; смена темы обновляет фокус. | End-to-end: follow-up, короткое продолжение, смена услуги, CLARIFY, «Не знаю», новая сессия, устаревшее действие и replay. Сервер не заставляет модель повторно выбирать известное. |
| 3 — цены и объём | Короткий обзор утверждённого направления, несколько предложений одной услуги, цена/единица/существенные условия и уточнение объёма без требования «ровно одно предложение». | Имплантация, протезирование, All-on-4 и иные услуги на общих данных; один зуб, несколько, челюсть, неизвестный объём, отсутствие цены; Checker и отдельный Cursor review. |
| 4 — единый ответ | В одном frozen plan соединяются живой текст и проверенные цена, политика или контакт. Независимая доступная часть сохраняется при сбое другой; renderer не повторяет длинные пакеты и этапы без запроса. | Реальные JSON/SSE endpoint и widget harness: информация+цена, цена+контакт, сложный multipart и follow-up; проверены смысл, текст, UI, состояние, логи и отсутствие post-plan отбора. |
| 5 — демо-приёмка | Полная offline-матрица, replay/обрыв SSE, tenant/lead/privacy, запрет старого runtime, качество и задержка. Live-вызовы — только по отдельному разрешению владельца. | Checker, финальный Cursor review, доказанный сквозной результат; затем ручная проверка владельца в локальном виджете. HTTP 200 или зелёный unit-тест сами не закрывают этап. |

Перед этапом 1 отдельно инвентаризировать незакоммиченный D2 WIP в основной
папке и предъявить владельцу, какие его части входят в baseline новой
кодовой задачи. Не сбрасывать и не переключать грязную папку, не переносить
WIP целиком по умолчанию.

На каждом этапе исполнитель Terra/Sol проверяет отсутствие костылей,
подгонки тестов, старого semantic runtime, смысловых regex и решений вне
утверждённой архитектуры. При архитектурном конфликте подключает Astra и
останавливает изменение до решения владельца. Один крупный этап — один
последовательный чат, без конкурирующих задач над одним деревом. Каждый
связный checkpoint получает независимый Checker; отмеченные рубежи —
отдельный Cursor review. Без явного решения владельца запрещены расширение
архитектуры, новый strict gate, live/provider calls, merge и deploy.

Проверяются семейства запросов, а не только знакомые фразы: свободный
разговор, услуга и выбранный раздел; продолжение и смена темы; общая и
точная цена, несколько предложений, CLARIFY, объём и «Не знаю»;
политика, контакты, запись, неизвестный бренд/термин; смешанные вопросы;
`request_id`, новая сессия, SSE disconnect и JSON/SSE parity. Для каждого
хода проверяются смысл от UI до модели и обратно, ответ, UI, проверенные
блоки, session delta и сохранённый replay. Сложный multipart может явно
отложить одну часть и ответить на доступную, но не заявлять, что ответил
на всё.

## Историческая переоценка — 2026-09-23

На ветке `codex/demo-d2-service-volume`, HEAD `298f094c2a26ce17f63657dc52c447511b802529`,
локальные `/ask` и `/ask/stream` уже вызывают D2; widget отправляет
`POST /ask/stream` на том же origin. Исторические строки ниже про ещё не
подключённый HTTP, следующий CP5-M1 и обслуживающий widget legacy route
описывают прежний checkpoint. Этот раздел фиксирует порядок, действовавший
на дату снимка; новый порядок находится выше;
исторические checkpoint и их Ledger не переписываются задним числом.
Это документационный checkpoint по прямому решению владельца от 2026-09-23:
составить план ремонта и закрепить отсутствие реальных диалогов и запрет
временного сохранения старой архитектуры.

В production нет бота и пользователей; реальных диалогов сейчас нет.
Локальный widget — испытательный, а не канал текущего обслуживания.
Владелец проверяет первые ответы в нём вручную **только после окончательного
подключения новой архитектуры**. Нельзя сохранять Composer, sales_fast,
старую ordinary memory или semantic selectors даже временно ради
«ответов реальным пользователям сейчас». Существующий локальный маршрут
не является целью совместимости. Сохраняются tenant, lead/privacy, typed UI
ownership и transport boundaries из [execution lock](DEMO_D2_EXECUTION_LOCK.md).

Аудит текущего D2 обнаружил несколько независимых сбоев на обычных вопросах:

| Сценарий | Доказанный результат | Проблемная граница |
|---|---|---|
| Приветствие | `ANSWER` с `other` принят parser, отвергнут D2 как `d2_experiment_single_price_required` | Prompt/parser → D2 gate |
| Приживаемость имплантов | Две изолированные live-пробы: `content_ref` есть, `topic_id`/`service_id` нет; `d2_experiment_resolved_topic_required` | Typed focus → D2 gate |
| Продолжение | `CLARIFY` принят parser, отвергнут D2 как `d2_experiment_single_price_required` | Prompt/parser → D2 gate |
| Информация об all-on-4 | Завершённый ход дал общий пробел: `content_ref` отсутствовал, запрос ушёл в availability | Model view/prompt → D2 materialization |
| Политика детского приёма | Завершённый ход дал общий пробел при существующей authored policy | Model view/prompt → policy resolution |

В новых пробах цена направления и контактный вопрос по одному разу прошли.
Это не показатель общей надёжности. Исторический `d2_turn_failed` в реальной
вкладке пока не имеет установленной причины. `app.py` сводит разные
`ValueError` к `d2_invalid_turn`, неожиданные исключения — к
`d2_turn_failed`; SSE может вернуть HTTP 200 с `event: error`.
Структурный факт: из 58 документов tenant model view prompt передаёт
только один документ об имплантных системах; документ all-on-4 и
authored policy authority в prompt отсутствуют. Зелёные offline-тесты с заранее правильными
fake envelopes не проверяют эти границы.

### Последовательность ремонта и ворота выхода

| Шаг | Результат | Обязательная проверка до следующего шага |
|---|---|---|
| R1 — единый контракт | Для greeting, свободного разговора по базе, CLARIFY, price, policy, contact, multipart и continuation явно определены допустимые typed формы. Prompt, production parser, focus binding и D2 gate согласованы. Техническая ссылка на документ может доказывать источник, но не определяет тему или форму обычного ответа. Не добавлять отдельные правила под буквальную фразу. | Таблица форм ниже и причины отказа проверены на допустимых и неполных envelopes через production parser и общий D2 turn. Тест вызывает настоящий `build_d2_d1r_messages` на штатном tenant snapshot и доказывает, что весь утверждённый корпус клиники присутствует в одном сообщении модели. Заранее вписанный `content_text` в fake envelope не доказывает доступ модели к материалу. Недопустимые формы отвергаются точным безопасным кодом. |
| R2 — FullContext tenant prompt | Реализовать один tenant-bound prompt/response contract: в единственном вызове модели есть полный утверждённый текст всех документов клиники, а также typed IDs политик, цен и одобренных коммерческих фактов. Не применять каталог заголовков, RAG, выбор или подгрузку отдельных документов. Соблюсти реальный лимит API и один provider call; не добавлять старый lexical selector, второй semantic prompt или угадывание refs. | На demo tenant в построенном сообщении есть полный утверждённый корпус, включая all-on-4 и приживаемость; размер измерен на реальных данных и сопоставлен с лимитом API. Policy, price и commercial IDs принадлежат tenant snapshot. Выдуманный/чужой/устаревший ref отвергнут как служебная ссылка, но его отсутствие не меняет смысл нормального разговора. |
| R3 — свободный диалог и точные исключения | Обычный разговор о клинике остаётся свободным ответом модели по FullContext: объяснение, сравнение, сомнение, приветствие и вопрос «делаете ли…» не превращаются в шаблон из-за `content_ref`, `kind` или отсутствующей технической ссылки. Код строго обрабатывает только цену/коммерцию, политику, контакты, проблемные обращения, leadflow, спам, явную ситуацию пациента и tenant/UI границы. Для услуги и бренда: доступное в базе — свободный ответ; явно запрещённое политикой — утверждённая заглушка; запрещённое с альтернативой — заглушка и разрешённые кнопки; ясное название вне утверждённых данных — «нет информации»; неясное название — короткое уточнение. | Общий D2 turn возвращает свободный grounded ответ для доступной услуги независимо от вопроса «расскажите» или «делаете». Смешанные сочетания строгой policy-ветки с другой самостоятельной частью не входят в R3-A: до отдельного общего механизма D2 завершает только строгую policy-часть. Это явно зафиксированная временная граница этапа, а не доказательство полноты ответа; позже её заменит общий составной ответ. Нет ложного общего пробела при доступном материале; нет silent success без доказанной точной части. |
| R4 — диагностика и хранение | Каждый отказ имеет этап, класс и allowlisted reason code; HTTP/SSE исход классифицирован после последнего события. Успех означает сохранённый итог, а не HTTP 200. | Искусственные parser/gate/provider/store ошибки различимы без raw модели, PII и ключей. Failed turn не меняет ordinary state; replay не делает новый provider call. |
| R5 — сквозная offline приёмка | Raw fake provider → production parser → D2 binding/materializer → store → `/ask` и `/ask/stream`; JS harness использует настоящий `api.js`/`widget.js`. Fake envelopes включают правдоподобные неполные варианты модели, не только заранее правильные. | Матрица ниже, tenant/lead/privacy, typed actions, replay, no-legacy sentinels; тестовые БД и логи только временные. Независимый Checker проверяет связный checkpoint. |
| R6 — ограниченная live-проверка | Настоящая модель проходит ту же матрицу с несколькими формулировками и продолжениями на временных БД. | Владелец заранее разрешил точный набор и жёсткий бюджет; автоматические retries выключены или входят в бюджет. Разделены технический успех, верность источнику и полнота ответа. Один успех не закрывает сценарий. |
| R7 — окончательное подключение widget | Один D2 runtime обслуживает оба endpoint и окончательный widget flow; заменённые normal-dialogue механизмы недостижимы или удалены. Нет временного старого маршрута. | Автоматический браузерный harness, полный endpoint regression, dependency/sentinel check, нужный CI и независимый Checker. После этого владелец начинает ручную проверку первых ответов widget. |

Минимальная таблица форм R1 — классы входа, а не тесты буквальных фраз:

| Форма после production parser | Что обязан определить единый D2 контракт |
|---|---|
| Корректный `CLARIFY` с обязательными полями | Уточнение завершается и сохраняется; не попадает в отказ цены. |
| `ANSWER` + `other` с текстом | Свободный короткий ответ допустим для нормального обращения, например приветствия. Спам, флуд и посторонние обращения остаются отдельной защитной границей; текст не маскируется ценовым отказом. |
| `ANSWER` + полностью пустой `other` при `commercial_intent=none`, без topic/service/content/policy/contact refs | Офтопик по точной форме. Offline проверить эту ветку отдельно от допустимой формы приветствия с текстом; из одинаковых typed полей D2 не выводит скрытый смысл исходной фразы. |
| `content` с `content_ref`, без `topic_id`/`service_id` | Техническая ссылка проверяется на tenant ownership, но отсутствие темы не даёт падения и ссылка не управляет смыслом свободного ответа. |
| `content` без `content_ref`, с доступным `service_id` | Свободный ответ по FullContext и на «расскажите», и на «делаете ли». Отсутствие ссылки не превращает вопрос в availability gap. |
| Ясно названная услуга/бренд вне утверждённых данных; неясный термин | Для ясного названия — строгий честный ответ «нет информации»; для неясного — короткое уточнение. Различие приходит из typed результата модели, без повторного поиска по сырому тексту пользователя. |
| `price` с `service_id`, без `topic_id` | Единая проверка связи услуги с темой или безопасное уточнение; проверить нынешний `request_topic_id_missing`. |
| `content_ref` с допустимым синтаксисом `*.md`, но отсутствующий в tenant snapshot | Проверить существование и tenant ownership до публикации; parser проверяет форму имени, не наличие файла. |
| `clinic_policy`, `contact`, `booking` | Проверить соответственно authored policy ID, code-owned contact и вход в заявку/блокировку по правилам lead; не отправлять booking в ценовой gate. |

Для каждой строки R1 проверяет подходящие положительный и отрицательный
варианты формы. Некоторые ветки имеют дополнительные условия (directory,
promotion, multipart, lead), поэтому таблица не утверждает, что каждый JSON
данного `kind` всегда пойдёт одним маршрутом. Fake envelope проверяет D2
после модели; тест настоящего prompt builder и происхождения текста —
самостоятельные ворота. Успех parser или HTTP 200 без проверенного результата
не закрывает строку. Выбирает ли живая модель форму приветствия вместо
пустого `other`-офтопика, проверяется в R6 на реальных формулировках; offline
fake envelopes этого не доказывают.

R2 закрепляет FullContext до начала R3: один вызов получает весь утверждённый
текст tenant-а, а не список документов, заголовки или выбранные разделы.
Измеряется полный вход в токенах по частям и сопоставляется с реальным лимитом
настроенного API с резервом под ответ. Нельзя заменить FullContext на RAG,
выбор ссылки или подгрузку материала по запросу. Второй LLM-вызов, старый
semantic selector и lexical selector не являются обходом ограничения.
Техническая ссылка может быть проверенным доказательством происхождения, но
не является разрешением на доступ к содержанию и не меняет смысл ответа.

Матрица R5/R6/R7: приветствие; сведения об all-on-4 и приживаемости;
свободное сравнение и сомнение; цена направления и конкретной услуги;
контакты; политика клиники; проблемное обращение; уточнение при
неоднозначности; доступная услуга в вопросах «расскажите» и «делаете ли»;
явно запрещённая услуга; запрещённая услуга с альтернативой; ясное
отсутствующее название и неясный термин; follow-up той же темы и смена темы; typed UI action; replay/обрыв
SSE. Для каждого хода проверять не только статус и UI, но и сохранность всех
частей смешанного ответа, источник утверждений, цену/единицу, действия,
состояние и число вызовов/effects.

Исторически следующим code checkpoint считался R3-B: общий составной ответ без новых смысловых
маршрутов. Обычная FullContext-проза остаётся ответом модели, а typed цена,
контакты и явно запрошенный одобренный коммерческий факт добавляются из bound
tenant data в том же frozen plan. Проверка должна пройти через один общий D2
turn для «свободная информация + цена + контакт» и для «свободная информация
и точный коммерческий факт»; адрес, телефон, сумма и коммерческий факт не берутся
из текста модели. Строгие policy-части пока
сохраняют явно описанную границу R3-A. R3–R4 можно делить на небольшие связные checkpoint,
но R7 и первое ручное испытание владельца не объявлять выполненными до
полной матрицы. Ни один шаг не разрешает возврат старой архитектуры.

## Исторический план checkpoint

Следующий текст фиксирует прежний порядок и состояние на указанном baseline.
Для новых работ действует раздел «Порядок этапов и ворота» в начале файла:
этапы 0–5. Исторические R1–R7, включая временные границы R3-A/R3-B,
не задают порядок, правила или допустимые ограничения новых задач.

Статус на том checkpoint: **действовавший тогда план исполнения**, обновлён 2026-09-23 на ветке
`codex/demo-d2-service-volume` от baseline `4f7ac07`. Подтверждённые
внутренние CP5-сценарии перечислены в Ledger; общий агрегированный offline
CP5-REG прогон дал **100 passed**, независимый Cursor Checker — **PASS**.
Первая локальная правка CP5-B04i получила Cursor **REJECT** и заменена
упрощённым CP5-B04s по решению владельца: focused recheck **6 passed**, общий
offline-набор **105 passed**, независимый Cursor Checker — **PASS**. На том
историческом checkpoint HTTP и виджет обслуживал локальный legacy route.

Это единственная действующая roadmap D2. Историческая
`DEMO_D2_REBUILD_ROADMAP.md` объясняет прежний план, но не задаёт порядок
работы. Доказанные факты записываются только в
[DEMO_D2_CHECKPOINT_LEDGER.md](DEMO_D2_CHECKPOINT_LEDGER.md), а жёсткие
запреты — в [DEMO_D2_EXECUTION_LOCK.md](DEMO_D2_EXECUTION_LOCK.md).

Изменить порядок, добавить продуктовый сценарий или ослабить запрет этой
карты можно только отдельным checkpoint с явным решением владельца. Новый
чат, новый исполнитель или более поздний prompt не отменяют эту карту.

## Цель и пределы

Конечный результат — один D2 runtime обычного диалога. Он получает запрос
через `/ask` и `/ask/stream`, использует один D1R prompt/parser, один owner
ordinary dialog state, typed tenant data и один финальный response/UI plan.

После переключения normal answer path не вызывает Composer, `sales_fast`,
legacy semantic selectors или старую ordinary memory. Нельзя сохранить их
как fallback, режим совместимости, обработчик «неизвестных» вопросов или
вторую ветку HTTP.

Запрещён **legacy fallback** и временное сохранение старого normal-dialogue
runtime ради текущих ответов: передать вопрос Composer / `sales_fast`
или выбрать runtime по ошибке D2 нельзя. Разрешён **неполный ответ внутри того же
D2 plan**: сохранить проверенные части из текущего tenant snapshot; показать
карточку услуги или направления только если модель уже вернула typed ID и
запись есть в снимке; иначе утверждённый пробел, одно уточнение или
консультация по правилам CTA — без угадывания услуги «на всякий случай».
Это не второй runtime и не обход ошибки.

В production нет бота и нет пользователей. Поэтому локальный legacy runtime
не является тем, что надо защищать. Но сохраняются tenant isolation,
lead/privacy, заявки, typed UI ownership и transport защиты. Нельзя заменять
их самодельными упрощениями.

Объём ограничен действующими
[D2 target contract](DEMO_D2_TARGET_CONTRACT.md),
[acceptance](DEMO_D2_ACCEPTANCE.md),
[product decisions](DEMO_D2_PRODUCT_DECISIONS.md) и
[service scope](DEMO_D2_SERVICE_VOLUME.md). Если там нет правила для нового
видимого поведения, исполнитель останавливается и просит решение владельца
с двумя примерами «до/после».

## Текущее доказанное состояние

На baseline `4f7ac07` внутренний `run_d2_dialogue_turn` содержит CP5
commercial, content, continuation, multipart, availability, recovery,
terminal, lead/privacy, spam, directory, CTA и off-topic checkpoint из Ledger.
Их fake provider проходит production D1R parser; ordinary state/result
записывается только в `D2DialogueStore`, а PII/активная заявка остаются у
существующего lead-owner. Точный client-binding probe через
`session.current_session_client_id()` нужен для active-lead short-circuit
даже без `lead_bridge`; он не читает ordinary memory. Агрегированный CP5-REG
проверил 100 тестов вместе, но не доказывает HTTP, браузерный widget,
общую live-модель или все полные A/B семьи. Конкретные доказанные срезы и
оставшиеся пробелы ведутся в Ledger.

CP5-B04s добавляет сквозные offline-тесты двух информационных вопросов как
на разные темы, так и на разные услуги одной темы. Общий D2 route принимает
обе части без единого topic focus. Для любых двух отдельных md действует одно
правило: оба ответа сохраняются, follow-up и видео отдельных документов не
показываются; один готовый comparison md сохраняет свой UI. Различать
«сравнение» и «два вопроса» отдельным полем или эвристикой не нужно (D2-072).
Это локальное доказательство с fake provider, не live проверка распознавания
моделью и не HTTP cutover; Cursor Checker подтвердил CP5-B04s без P0/P1,
включая tenant/lead границы (3 passed). B05 не требует новых ценовых данных: в demo
pricebook уже есть предложения Implantium, Impro и Nobel Biocare. Локальный
CP5-B05 связал typed brand ID с активными ценами этого бренда,
существующим каталогом стран и документом об имплантах; Osstem берётся из
утверждённой tenant policy, неизвестный бренд получает честный пробел.
Независимый Cursor Checker подтвердил B05: P0/P1 нет, B05 9 passed,
assembled CP5 105 passed, lead/privacy 10 passed. Два известных component
failure в `test_d2_demo_snapshot.py` не являются B05-регрессиями.
Это пока не доказательство распознавания бренда настоящей моделью и не HTTP
cutover. Launch-edge checkpoint перед CP6a проверил A06 (составной вопрос,
затем неоднозначное «сколько это?»), A09 (три зуба на обеих челюстях с
опубликованной единицей цены), B07 (общая информация по документу и осмотр
для личной совместимости), B02 (неизвестный термин дважды). Для B02 внутри
одного D2 route добавлен узкий typed fail-closed: одно уточнение, затем
честный пробел и консультация через существующий `clarify_pending`; второй
ordinary state и специальные правила по терминам не добавлены. Focused
offline: 4 passed; общий assembled CP5 вместе с этим файлом: 111 passed.
Это fake-provider proof внутреннего route, не live распознавание модели и
не полный персональный B07. Независимый Cursor Checker подтвердил CP5-EDGE:
P0/P1 нет, focused 4 passed, tenant/lead/privacy 11 passed, независимо
собранный CP5 + EDGE 109 passed. Общий `clarify_pending` может дать честный
gap после unrelated CLARIFY; для узкого сценария это принято как fail-safe.
Следующий checkpoint — CP6a `/ask`.

## Неподменяемый порядок checkpoint

| ID | Наблюдаемый результат | Границы изменения | Приёмка и доказательство | После checkpoint ещё не готово |
|---|---|---|---|---|
| R0 — governance | Lock, эта roadmap, Ledger и master prompt Cursor существуют и согласованы | Только документы процесса | Cursor читает их до каждого значимого review | Любой D2 сценарий и HTTP путь |
| CP1 — единый prompt-contract | Production parser принимает расширенный typed D1R, нужный A08; prompt описывает те же поля | Существующий prompt-contract, parser/schema и offline tests; без data, HTTP, widget, live и новых сценариев | Raw ответ проходит **production parser**, не вручную созданный envelope; нет второго prompt/parser | Реальная модель, approved data, HTTP и пользовательский маршрут |
| CP2 — approved demo tenant data | Production tenant loader читает утверждённые direction-level данные для A08 | Штатный demo tenant pack/loader и tests; без test-only data binder | Цена/единица/условия берутся из tenant snapshot; tenant isolation проверяема | Настоящая модель, HTTP, widget и остальные сценарии |
| CP3 — ограниченный live A08 | Та же внутренняя D2 entry получает два последовательных ответа настоящей модели и соблюдает D1R/continuity contract | Prompt, существующая D2 entry, изолированная harness; без HTTP/widget и новых product rules | Только после отдельного разрешения владельца: точные фразы, модель, data version, жёсткий call budget; проверяется структура/refs/continuity | Полная оценка качества, нагрузка, HTTP и готовность всего бота |
| CP4 — D2 common turn completion | Внутренний общий D2 route завершает обычный ход с одним owner state, result/replay и обязательными lead/privacy effects | D2 turn service, typed state/result store, tenant snapshot, lead/privacy bridge и offline tests; legacy не расширяется | Применимые C04–C06, C09–C10: tenant refs, атомарность, replay, isolation, один effect | HTTP/SSE cutover, widget и непокрытые пользовательские семьи |
| CP5 — D2 сценарии через общий route | A01–A12 и применимые B01–B17 проходят один внутренний D2 route, не набор helper-тестов | D2 contracts/resolvers/materializer/renderer, штатные tenant data и tests; checkpoint определяется новым механизмом, а не названием услуги или комбинацией блоков | У каждой семьи есть acceptance ID; 2–3 сценария можно собрать вместе только на одном уже очерченном механизме; второй ход берёт контекст из настоящего предыдущего D2 turn; raw-text semantic selector не добавлен | HTTP/SSE/widget и доказательство недостижимости legacy от реального входа |
| CP6a — `/ask` | Реальный JSON endpoint вызывает общий D2 turn и отдаёт сохранённый final result/UI | Ingress, request identity, lead/privacy, JSON adapter и offline endpoint tests; без fallback | Raw fake D1R → production parser → D2 store; repeat/conflict/concurrency, tenant isolation, active lead/privacy/effect и invalid provider; C05/C07/C08/C09 sentinels и dependency check на `/ask` | SSE, браузерный widget, удаление legacy файлов |
| CP6b — `/ask/stream` | Реальный SSE endpoint использует тот же D2 turn и тот же final result | SSE transport и offline parity/replay tests поверх CP6a; без второго semantic route | JSON↔SSE parity по text/UI/actions/state, disconnect/replay с тем же request ID без provider/effect, terminal/error events и no post-freeze writes; C05/C08/C09 sentinels на `/ask/stream` | Браузерный widget, удаление legacy файлов |
| CP6c — widget | Существующий браузерный клиент отправляет D2 actions и показывает один сохранённый результат | Widget request identity, pure UI wire projection, JS/offline browser harness; без old presentation decision | Реальный widget/api код: current typed click работает, stale/foreign/forged отклоняются; повтор после обрыва до/после UI сохраняет ID и даёт один bubble/lead effect; новый ход получает новый ID; обе endpoint sentinel-проверки остаются зелёными | Физическое удаление legacy файлов и итоговый regression/live набор |
| CP7 — удаление legacy | Composer/sales_fast semantic runtime, selectors, старая ordinary memory и их тесты удалены либо отсутствуют как normal-dialogue механизм | Только явно перечисленные legacy files/imports/wiring/tests/docs; не удалять tenant/lead/privacy/transport защиты | C08: dependency check и endpoint sentinels; назначенные offline regressions проходят без legacy imports | Merge/deploy; они не входят в D2 rebuild |
| CP8 — final evidence | Есть единый evidence-pack: required offline acceptance, ограниченный live набор, Cursor PASS и актуальный Ledger | Только тесты, доказательства и документы уже сделанного D2 | A01–A12, применимые B/C, C01–C10, latency/cost facts и known limits перечислены явно | Production deployment: он не в scope и не совершается автоматически |

### Приёмка CP6 по порядку доставки

1. **CP6a `/ask`:** offline endpoint test посылает raw fake D1R через
   настоящий parser, tenant snapshot и `run_d2_dialogue_turn`, затем сверяет
   response/UI и сохранённый result/state. Проверяет idempotent replay того же
   `(tenant, sid, request_id, payload)` после reopen store, конфликт и два
   конкурентных хода, разные tenant с одним sid, invalid provider и отказ
   commit. Отдельный путь проверяет booking→имя→pending-вопрос→телефон,
   отсутствие PII в provider input/ordinary store и не более одного effect;
   active lead не попадает в provider. Fail-on-call sentinels для legacy
   semantic функций и transitive dependency check действуют на успешный,
   ошибочный и lead/typed-action запрос. Proof: `test_d2_http_contract.py`,
   `test_d2_http_scenarios.py`, `test_d2_no_legacy_path.py` и назначенные
   lead/privacy/tenant offline regressions.
2. **CP6b `/ask/stream`:** те же входные payload и request ID дают после
   завершения одинаковые final text/UI/actions и одинаковую
   ревизию памяти с `/ask`; SSE отличается только transport framing. Обрыв
   до/после final с повтором через любой endpoint возвращает сохранённый
   result с 0 новых provider/effect calls. Invalid provider и transport error
   имеют один final/error outcome без late mutation; C08 sentinels и
   dependency check применяются к реальному SSE пути. Proof: те же HTTP
   contract/scenario/no-legacy tests с параметрами JSON↔SSE.
3. **CP6c widget:** JS harness использует настоящий `static/widget/api.js` и
   `widget.js`, а не мок UI projection. Проверяет один logical request ID
   для retry при обрыве до UI и после UI, новый ID для нового хода с тем же
   текстом, один bubble и отсутствие повторной заявки. Typed scope/lead
   action допускается только из сохранённой актуальной D2 UI projection;
   чужой, устаревший и forged action отклоняются. Text, CTA и secondary UI
   совпадают с сохранённым plan, включая terminal без CTA. Proof:
   `test_d2_widget_replay.py`, JS harness, оба HTTP contract/no-legacy набора.

Каждый шаг получает собственный review и Ledger evidence. CP6b начинается
после CP6a PASS, CP6c — после CP6b PASS. Текущий checkpoint CP5-REG не
выполняет HTTP cutover.

Нельзя считать CP5 готовым по unit/seam-тестам, нельзя перейти к CP6 с
legacy fallback, нельзя удалить runtime до того, как его заменяет D2 endpoint
route. Внутри CP4–CP5 разрешены малые checkpoint, но каждый имеет
ACCEPTANCE, D2 ROUTE, LEGACY IMPACT и Cursor PASS.

### Исторический порядок внутри оставшегося CP5

Аудит [DEMO_D2_SCENARIO_MARKETING_AUDIT.md](DEMO_D2_SCENARIO_MARKETING_AUDIT.md)
зафиксировал, что продолжать по одной услуге нельзя: acceptance-пример не должен
становиться production-веткой. На том checkpoint порядок был таким:

1. **CP5-M1 — commercial data contract:** две authored-формы одного promo fact,
   один service commercial profile (promo_refs ≤2, необязательный один пакет
   усилителя, необязательный один пакет «Также») и compatibility groups с
   готовым текстом. Используется существующий tenant loader/snapshot; второй
   data contract запрещён. Старые scenario rules не переносятся.
2. **CP5-M2 — common commercial plan:** один D2-native resolver формирует
   short promo, необязательный один пакет усилителя, необязательный один пакет
   «Также» и compatibility blocks до freeze. Legacy marketing selector
   недостижим из D2 route.
3. **CP5-M3 — assembled A02/A11/B08:** цена использует короткие additions,
   прямой запрос об акции — полную форму, несовместимость — authored alternatives.
   Пакет допустим только если M2 остаётся одним механизмом без разных state/rules;
   иначе он делится до реализации.
4. **CP5 content / direct-fact lookup:** утверждённые информационные
   материалы, гарантия как прямой факт, сравнение без самостоятельного
   медицинского вывода (A03/A04/B04/B15). Не растворять в multi-part и не
   подменять directory.
5. **CP5 continuation/choice:** обзор направления, кнопки объёма, same-topic
   и cross-topic carry, TTL, гипотеза ≠ факт.
6. **CP5 multi-part:** несколько независимых частей в одном сообщении;
   отложенный второй прайс; сломанная часть не убивает живые.
7. **CP5 availability/policy:** нет публичной цены, нет материала, явный
   отказ, ОМС/ДМС/дети — только authored policy.
8. **CP5 D2-native recovery:** неполный ответ внутри того же D2 plan:
   сохранить проверенные части снимка; карточку услуги/направления только при
   typed ID в данных. Это не продающий обход «всегда что-то показать» и не
   legacy fallback.
9. **CP5 terminal/lead/hard-stop:** заявка и текущая боль/жалоба; будущий
   страх боли сюда не входит.
10. **CP5 directory/UI:** врачи, протоколы, контакты, CTA/кнопки как правило
    сборки, не отдельный сюжет услуги.

    Пункты 4–10 — технические семейства checkpoint, не новые пользовательские
    сценарии. Дополнительные услуги уже доказанного механизма проверяются
    пакетно как data-driven cases, а не отдельным кодом.

D2-O4 закрыто D2-090: к услуге 0 или 1 пакет усилителя и 0 или 1 пакет
«Также». Перед CP5-M1 больше не требуется отдельное число усилителей. Старый
технический cap `4` не переносится.

## Правила доказательства

| Формулировка в отчёте | Разрешена, только если |
|---|---|
| «компонент реализован» | Есть код и focused offline test; это не означает сценарий |
| «внутренний маршрут проверен» | Тест идёт через общий D2 entry; состояние второго хода записано первым, не подставлено вручную |
| «подключено к входу бота» | Реальные `/ask` и `/ask/stream` вызывают D2 route; legacy fallback отсутствует |
| «проверено с настоящей моделью» | Есть разрешение владельца, call budget и зафиксированные результаты |
| «готово к закрытию D2» | Выполнены CP7 и CP8; это нельзя вывести из числа зелёных unit-тестов |

Fake provider допустим только как raw provider response, который разбирает
production parser. Он доказывает wiring, parser, state и deterministic data
resolution. Он не доказывает, что модель понимает русский вопрос, стабильно
выдаёт расширенный D1R или соблюдает refs; это назначение CP3.

## Контроль на каждом checkpoint

Перед записью Terra объявляет baseline, точный allowlist, ACCEPTANCE, D2
ROUTE, LEGACY IMPACT, OWNER DECISION, FUTURE SCOPE и точную команду offline
теста с временной БД, `BOT_LOG_DIR` и temporary tenant pack. Вне allowlist
не пишется ничего. Постоянный шаблон исполнителя —
[DEMO_D2_CODEX_EXECUTOR_PROMPT.md](DEMO_D2_CODEX_EXECUTOR_PROMPT.md).

Terra останавливается только при конфликте требований, новом видимом
пользовательском правиле или двух допустимых реализациях с разным видимым
поведением. Уже решённое contract/acceptance правило он применяет сам и
ссылается на него. Нельзя незаметно добавить strict refusal/gate, который
может скрыть нормальный ответ, или semantic inference из `patient_text`,
`assistant_text`, `patient_message`, `dialogue`, regex или возраста turn.

После изменений Terra запускает только assigned offline tests в изолированной
среде и добавляет в этот же незакоммиченный diff черновую строку Ledger с
доказанными фактами checkpoint. В строке указывается устойчивое имя
checkpoint, а не hash ещё не созданного commit. Cursor независимо читает весь
diff — код, tests и Ledger — по master prompt и выносит PASS/REJECT. После
REJECT исправляется находка и проверяется именно она с необходимой регрессией.
Один Cursor review нужен на законченный значимый checkpoint, а не на каждую
строку документации или мелкое исправление его замечания.

Только после Cursor PASS: exact staging, staged diff/stat/`diff --check`,
commit и push. Финальный отчёт сообщает фактический hash и готовый prompt
следующего checkpoint; он не меняет Ledger после PASS. Cursor не
перепроектирует D2 и не отклоняет работу за честно указанный FUTURE SCOPE.

## Исторический следующий шаг

CP1–CP4, CP5-A10a и CP5-B13a завершены с доказательствами из Ledger. После
Cursor PASS документационного аудита следующий разрешённый code checkpoint —
**CP5-M1: единый commercial data contract**. Он не подключает HTTP/widget,
не делает provider calls и не объявляет A02/A11/B08 собранными. Следующий шаг
определяется этой таблицей и Ledger, а не новой услугой или комбинацией
маркетинговых блоков.
